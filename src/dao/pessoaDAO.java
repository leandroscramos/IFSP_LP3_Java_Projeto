package dao;


public class pessoaDAO {
    /*
    dbConnect dbconn = new dbConnect();
    pessoaModel pessoamodel = new pessoaModel(txtCPF.getText(), txtNome.getText(), txtSexo.getText(), txtDataNasc.getValue(), cbFuncionario.getText(), txtEmail.getText(), txtCelular.getText());

    public void createPessoa(pessoaModel pessoamodel){

        dbconn.connection();
        try {
            PreparedStatement pst = dbconn.conn.prepareStatement("insert into pessoa (cpf, nome, sexo, datanasc, email, celular, isFuncionario) values(?,?,?,?,?,?,?)");
            pst.setString(1, pessoamodel.getCpf());
            pst.setString(2, pessoamodel.getNome());
            pst.setString(3, pessoamodel.getSexo());
            pst.setString(4, pessoamodel.getData_nasc());
            pst.setString(5, pessoamodel.getEmail());
            pst.setString(6, pessoamodel.getCelular());
            pst.setString(7, pessoamodel.getFuncionario());
            pst.execute();
            System.out.println("Dados inseridos com sucesso");
        } catch (SQLException ex) {
            System.out.println("Erro: "+ex);
        }
        dbconn.desconnection();
    }
    */

}
